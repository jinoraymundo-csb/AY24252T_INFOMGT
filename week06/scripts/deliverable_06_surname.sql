-- From your deliverable #3 submission:

-- #1 Sort them by name/title in descending order

-- #2 Sort them by name/title in alphabetical order, split them into 4 pages then take the 2nd page

-- #3 Sort them by name/title in reverse alphabetical order, split them into 3 pages then get the last page

-- #4 Rank them on your own and order them via `CHARINDEX()`

-- #5 Rank them on your own ordering via `CHARINDEX()`, split them to two pages, and get the 2nd page